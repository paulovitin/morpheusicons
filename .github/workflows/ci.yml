name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: Check, Format, Clippy & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libxkbcommon-dev libxkbcommon-x11-dev libwayland-dev
      - name: Format check
        run: cargo fmt --all -- --check
      - name: Clippy
        run: cargo clippy --all-targets --all-features -- -D warnings
      - name: Unit tests
        run: cargo test --lib --all-features
      - name: Integration tests
        run: cargo test --test morph_test
      - name: Doc tests
        run: cargo test --doc --all-features

#  test-os:
#    name: Tests (${{ matrix.os }})
#    runs-on: ${{ matrix.os }}
#    strategy:
#      fail-fast: false
#      matrix:
#        os: [macos-latest, windows-latest]
#    steps:
#      - uses: actions/checkout@v7
#      - uses: dtolnay/rust-toolchain@stable
#      - uses: Swatinem/rust-cache@v2
#      - run: cargo test --lib --all-features
#      - run: cargo test --test morph_test

#  deny:
#    name: Cargo Deny
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v7
#      - uses: EmbarkStudios/cargo-deny-action@v2

#  coverage:
#    name: Code Coverage
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v7
#      - uses: dtolnay/rust-toolchain@stable
#      - uses: Swatinem/rust-cache@v2
#      - name: Install system dependencies
#        run: sudo apt-get update && sudo apt-get install -y libxkbcommon-dev libxkbcommon-x11-dev libwayland-dev
#      - name: Install cargo-tarpaulin
#        run: cargo install cargo-tarpaulin
#      - name: Run coverage
#        run: cargo tarpaulin --all-features --lib --out xml --output-dir coverage/
#      - name: Upload coverage to Codecov
#        uses: codecov/codecov-action@v4
#        with:
#          files: coverage/cobertura.xml
#          fail_ci_if_error: false
#        env:
#          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

#  audit:
#    name: Security Audit
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v7
#      - uses: rustsec/audit-check@v2
#        with:
#          token: ${{ secrets.GITHUB_TOKEN }}

#  wasm:
#    name: WebAssembly Build
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v7
#      - uses: dtolnay/rust-toolchain@stable
#        with:
#          targets: wasm32-unknown-unknown
#      - uses: Swatinem/rust-cache@v2
#      - run: cargo check --target wasm32-unknown-unknown --features wasm --no-default-features
